"""
The client runs in ml process as the bridge of cross language client and game process.
"""
import json

from subprocess import PIPE, Popen

def ml_loop(execution_cmd, init_args, init_kwargs):
    pass
